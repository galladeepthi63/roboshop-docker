networks:
  default:
    external:
      name: roboshop
services:
  # docker run -d --name mongodb --network=roboshop mongodb:1
  mongodb:
    image: mongodb:1
    container_name: mongodb
  catalogue:
    image: catalogue:1
    container_name: catalogue
    depends_on:
    - mongodb